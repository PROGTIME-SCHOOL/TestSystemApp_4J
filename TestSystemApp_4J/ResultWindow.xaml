<Window x:Class="TestSystemApp_4J.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestSystemApp_4J"
        mc:Ignorable="d"
        Title="ResultWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <DataGrid Name="resultDataGrid" AutoGenerateColumns="True" CanUserAddRows="False"
                  IsReadOnly="True">
        </DataGrid>
        
        <!-- 
        <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Scores" Binding="{Binding Scores}" Width="*"/>
                <DataGridTextColumn Header="Start Date Time" Binding="{Binding StartDateTime}" Width="*"/>
                <DataGridTextColumn Header="End Date Time" Binding="{Binding EndDateTime}" Width="*"/>
            </DataGrid.Columns>
        -->
    </Grid>
</Window>
